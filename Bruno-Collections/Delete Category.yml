info:
  name: Delete Category
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/category/delete/6a46357e5b1f05846269816f"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
