info:
  name: View All - Admin Only
  type: http
  seq: 23

http:
  method: GET
  url: "{{baseUrl}}/support/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
