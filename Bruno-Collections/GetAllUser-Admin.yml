info:
  name: GetAllUser/Admin
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/users/all-user"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
