info:
  name: View Wishlist
  type: http
  seq: 20

http:
  method: GET
  url: "{{baseUrl}}/wishlist"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
