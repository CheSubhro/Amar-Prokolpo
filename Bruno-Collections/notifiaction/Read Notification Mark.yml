info:
  name: Read Notification Mark
  type: http
  seq: 13

http:
  method: PATCH
  url: "{{baseUrl}}/notifications/read/<notification_id>"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
