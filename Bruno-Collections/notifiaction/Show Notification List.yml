info:
  name: Show Notification List
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/notifications/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
