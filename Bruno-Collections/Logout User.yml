info:
  name: Logout User
  type: http
  seq: 14

http:
  method: DELETE
  url: "{{baseUrl}}/users/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
