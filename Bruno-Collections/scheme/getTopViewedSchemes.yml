info:
  name: getTopViewedSchemes
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/scheme/top-viewed"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
