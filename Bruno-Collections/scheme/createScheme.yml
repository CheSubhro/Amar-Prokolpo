info:
  name: createScheme
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/category/create"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
