info:
  name: Get All Scheme
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/scheme/all/?isPublished=false"
  params:
    - name: isPublished
      value: "false"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
