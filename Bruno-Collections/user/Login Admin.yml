info:
  name: Login Admin
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/users/login"
  body:
    type: json
    data: |-
      {
        "username": "admin_user",
        "password": "{{adminPassword}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
