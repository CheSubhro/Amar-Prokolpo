info:
  name: GetCurrentAdmin
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/users/current-user"
  headers:
    - name: ""
      value: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
