info:
  name: LogActivity
  type: http
  seq: 24

http:
  method: GET
  url: "{{baseUrl}}/users/logs"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
