info:
  name: ForgotPassword
  type: http
  seq: 18

http:
  method: POST
  url: "{{baseUrl}}/users/forgot-password"
  body:
    type: json
    data: '{ "email": "admin@example.com" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
