info:
  name: Create Category
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/category/create"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
