info:
  name: getCategoryBySlug
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/category/shiksha-sahayata-prakalpa"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
