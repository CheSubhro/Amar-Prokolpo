info:
  name: Get All Category
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/category/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
