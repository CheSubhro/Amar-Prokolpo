info:
  name: getPendingReviews
  type: http
  seq: 17

http:
  method: GET
  url: "{{baseUrl}}/reviews/admin/pending"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
