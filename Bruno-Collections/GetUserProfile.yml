info:
  name: GetUserProfile
  type: http
  seq: 22

http:
  method: GET
  url: "{{baseUrl}}/users/profile/admin_user"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
